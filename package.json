{
  "name": "reentryapp",
  "version": "0.1.0",
  "description": "Web app supporting reentry into the community after incarceration.",
  "main": "server.js",
  "engines": {
    "node": ">=18.11.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "echo \"No build step (static Express app)\" && exit 0",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "type": "commonjs",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.7.0",
    "helmet": "^8.3.0"
  },
  "overrides": {
    "path-to-regexp": "^8.4.2",
    "qs": "^6.15.2"
  }
}
