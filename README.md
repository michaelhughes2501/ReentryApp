# ReentryApp

A web app supporting reentry into the community after incarceration.

## Quick start

```bash
npm install
npm start          # http://localhost:3000
```

Open in VS Code and press **F5** to run with the debugger attached.

See [`CLAUDE.md`](./CLAUDE.md) for a project map and conventions.
